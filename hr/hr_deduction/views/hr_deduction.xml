<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="hr_deduction_sequence" model="ir.sequence">
            <field name="name">hr.deduction.sequence</field>
            <field name="code">hr.deduction</field>
            <field name="prefix">DED</field>
            <field eval="1" name="number_next"/>
            <field eval="1" name="number_increment"/>
            <field name="padding">5</field>
        </record>
    </data>
    <data>
        <!-- Views -->
        <!-- Form View -->
        <record id="hr_deduction_inform_view" model="ir.ui.view">
            <field name="name">hr.deduction.form</field>
            <field name="model">hr.deduction</field>
            <field name="arch" type="xml">
                <form string="Deduction">
                    <header>
                        <field name="state" widget="statusbar"
                               statusbar_visible="f_approve,approved,done"/>
                        <button name="approve" string="Approve"
                                type="object" class="btn-primary"
                                attrs="{'invisible': [('state', '!=', 'draft')]}" groups="hr.group_hr_manager"/>
                        <button name="reject" string="Refuse" type="object"
                                class="btn-primary"
                                attrs="{'invisible': [('state', 'not in', ['draft'])]}" groups="hr.group_hr_manager"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <field name="name" required="0"
                                       placeholder="Reference ...."/>
                            </h1>
                        </div>
                            <group col = '4'>
                                <field name='employee_id'
                                       options="{'no_quick_create': True, 'no_create_edit' : True}"
                                       attrs="{'readonly': [('state', '!=', 'draft')]}"/>
<!--                                <field name='late_minutes'/>-->
                                <field name='date' attrs="{'readonly':[('state','!=','draft')]}"/>
                                                                <field name="amount" attrs="{'readonly':[('state','!=','draft')]}"/>

                                <field name="company_id" attrs="{'readonly':[('state','!=','draft')]}"/>
                            </group>
                            <group>
                                                                <field name="description" attrs="{'readonly':[('state','!=','draft')]}"/>

                            </group>
                    </sheet>
                    <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
                </form>
            </field>
        </record>

        <record id="hr_deduction_in_view_search" model="ir.ui.view">
            <field name="name">hr.deduction.search.view</field>
            <field name="model">hr.deduction</field>
            <field name="arch" type="xml">
                <search string="Search Deduction records">
                    <group expand="0" string="Group By">
                        <filter string="Employee" name="group_employee" domain="[]"
                                context="{'group_by':'employee_id'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- Tree View -->
        <record id="hr_deduction_in_tree_view" model="ir.ui.view">
            <field name="name">hr.deduction.tree</field>
            <field name="model">hr.deduction</field>
            <field name="arch" type="xml">
                <tree string="Deduction">
                    <field name="name"/>
                    <field name="employee_id"/>
                    <field name="date"/>
                    <field name="amount"/>
                </tree>
            </field>
        </record>

        <!-- Actions -->
        <!-- overtime -->
        <record id="hr_deduction_in_action" model="ir.actions.act_window">
            <field name="name">Deduction</field>
            <field name="res_model">hr.deduction</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{
            'search_default_group_employee': 1,
            }
        </field>
        </record>


        <!-- MenuItems -->
        <menuitem id="menu_hr_deduction_in" name="Deduction"
            parent="hr.menu_hr_employee_payroll"
                  action="hr_deduction_in_action"
                  sequence="21"
                  />

        <record id="hr_payslip_inherited_view_hr_deduction_in" model="ir.ui.view">
            <field name="name">hr_deduction_in.Inherited.View</field>
            <field name="model">hr.payslip</field>
            <field name="inherit_id" ref="hr_payroll_community.view_hr_payslip_form"/>
            <field name="arch" type="xml">
                <field name="name" position="after">
                    <field name="deduction_ids" invisible="1"/>
                </field>
            </field>
        </record>


    </data>
</odoo>
